const subcommands = [require('./load'), require('./list'), require('./delete'), require('./validate')];

let _yargs;

module.exports = {
  builder: (yargs) => (_yargs = yargs) && yargs.command(subcommands).scriptName(''),
  handler: (argv) => !argv[1] && _yargs.showHelp(),
  command: 'extension',
  describe: 'Extension related commands',
};
