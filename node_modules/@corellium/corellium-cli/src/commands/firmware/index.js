const subcommands = [require('./load'), require('./list')];

let _yargs;

module.exports = {
  builder: (yargs) => (_yargs = yargs) && yargs.command(subcommands).scriptName(''),
  handler: (argv) => !argv[1] && _yargs.showHelp(),
  command: 'firmware',
  describe: 'Firmware related commands',
};
