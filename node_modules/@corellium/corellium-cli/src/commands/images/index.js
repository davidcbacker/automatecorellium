const subcommands = [require('./create'), require('./list'), require('./delete')];

let _yargs;

module.exports = {
  builder: (yargs) => (_yargs = yargs) && yargs.command(subcommands).scriptName(''),
  handler: (argv) => !argv[1] && _yargs.showHelp(),
  command: 'image',
  describe: 'Image related commands',
};
