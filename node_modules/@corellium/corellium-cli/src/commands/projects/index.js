const subcommands = [require('./create'), require('./list'), require('./delete'), require('./vpnConfig')];

let _yargs;

module.exports = {
  builder: (yargs) => (_yargs = yargs) && yargs.command(subcommands).scriptName(''),
  handler: (argv) => !argv[1] && _yargs.showHelp(),
  command: 'project',
  describe: 'Project related commands',
};
